# Order By

_Origem: `Consultas (queries) > PayoutRules > Order By` na collection Postman._

Os campos possíves de ordenação são os seguintes:

`id`: Ordena registros pelo ID específico.

`name`: Ordena registros pelo nome do payoutRule.
